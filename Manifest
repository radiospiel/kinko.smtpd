# which software must be installed
dependencies=kinko.mailqueue kinko.frontend kinko.connectivity os.golang

# which commands build the interface for this application?
exports=

# which permissions are exported?
permissions= 

# which permissions are required by this application?
requires=frontend.authenticate

# which daemons?
daemons=run
