dependencies kinko.mailqueue kinko.frontend kinko.connectivity golang

# which commands build the interface for this application
exports configure run

# which permissions are exported?
# permissions 

# which permissions must exist for this application?
requires frontend.authenticate
